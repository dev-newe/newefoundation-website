import { MissionVisionSection } from "@/assets/data/pages/Home";

const About = ({ data }: { data: MissionVisionSection }) => {
  console.log(data, "data");
  return <div>About</div>;
};

export default About;
