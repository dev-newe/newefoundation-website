"use client";
import { TeamSection } from "@/assets/data/pages/Home";

const Team = ({ data }: { data: TeamSection }) => {
  console.log(data, "data");
  return <div>Team</div>;
};

export default Team;
